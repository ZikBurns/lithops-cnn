syntax = "proto3";
package urlrpc;

// to compile
// python3 -m grpc_tools.protoc -I./ --python_out=. --grpc_python_out=. --pyi_out=. ./urlrpc.proto


service URLRPC {
  rpc Add (urlRequest) returns (urlResponse);
}

message urlRequest {
  repeated Dict finished = 1;
}

message urlResponse {
  repeated string urls = 1;
}

message Dict {
  string key = 1;
  string value = 2;
}
